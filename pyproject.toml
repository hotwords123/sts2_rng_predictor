[project]
name = "sts2-rng-predictor"
version = "0.1.0"
description = "Predict correlated Slay the Spire 2 RNG outputs from observed RNG call ranges."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.uv]
package = false
